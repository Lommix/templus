#[derive(Debug)]
pub(crate) enum Token {
    Operator,
    Expression,
    Variable,
    Literal,
    Keyword,
    Statement,
    Html,
    Comment,
    Punctuation,
}

#[derive(Debug)]
pub(crate) enum Keywords {
    If,
    Else,
    Endif,
    With,
    In,
    Set,
    For,
    EndFor,
    Define,
    Extend,
    Import,
    Block,
    Assign,
    NotEq,
    Eq,
    Gte,
    Gt,
    Lte,
    Lt,
    And,
    Or,
    Comma,
    Undefined,
}
